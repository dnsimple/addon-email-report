<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="">
    <meta name="author" content="">

    <title>DNSimple E-Mail reports</title>
    <link rel="stylesheet" href="<%= static_path(@conn, "/css/app.css") %>">
    <link rel="stylesheet" href="https://make-it-nice.dnsimple.com/css/dnsimple-tachyons-1.0.0.min.css">
  </head>

  <body class="dn-body">
    <div class="container">

      <header class="dn-navbar">
        <div class="dn-navbar-branding ph3">
          DNSimple E-Mail Reports
        </div>
        <div class="dn-navbar-nav container">
          <%= if @conn.assigns[:current_account] do %>
          <%= link("Logout", to: logout_path(@conn, :logout), method: :post) %>
          <% end %>
        </div>
      </header>

      <%= if info = get_flash(@conn, :info) do %>
      <div class="dn-alert-info"><span><%= info %></span></div>
      <% end %>
      <%= if error = get_flash(@conn, :error) do %>
      <div class="dn-alert-danger"><span><%= error %></span></div>
      <% end %>

      <main role="main">
        <%= render @view_module, @view_template, assigns %>
      </main>

    </div> <!-- /container -->
    <script src="<%= static_path(@conn, "/js/app.js") %>"></script>
  </body>
</html>
